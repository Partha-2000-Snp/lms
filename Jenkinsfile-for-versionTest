pipeline {
    agent any
    stages {
        stage('sonar analysis') {
            steps {
                def packageJson = readJSON file: 'package.json'
                def Version = packageJson.version
                echo "${Version}"
            }
        }
    }
}