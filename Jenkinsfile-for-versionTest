pipeline {
    agent any
    stages {
        stage('read json version for assigning tag to artifact') {
            steps {
                script {
                    def packageJson = readJSON file: 'webapp/package.json'
                    def Version = packageJson.version
                    echo "${Version}"
                }
            }
        }
    }
}